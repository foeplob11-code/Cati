# 학습 시작하기

**Kaggle이 주력, Colab은 보너스.** 두 곳이 Hugging Face Hub에 있는 같은
체크포인트를 이어받습니다.

| | 실효 성능 | 확보 시간 | 역할 |
|---|---|---|---|
| **Kaggle GPU T4 x2** | 26~32 TFLOPS | **주 30h 보장** | 주력 |
| Colab TPU v5e-1 | 40~50 TFLOPS | 주 1~2h (불안정) | 여유 있을 때 |

Colab이 더 빠르지만 무료 한도가 너무 적고 백그라운드 실행도 안 됩니다.
총 계산량은 Kaggle이 10~30배 많습니다.

⚠️ **두 곳에서 동시에 돌리지 마세요.** 서로 덮어씁니다.

---

# 준비 — 한 번만

## 1. Hugging Face 토큰

huggingface.co/settings/tokens → **New token** → **Write** 권한 → 문자열 복사

## 2. Kaggle에 등록

노트북 → **Add-ons → Secrets** → 이름 `HF_TOKEN`, 값 붙여넣기

---

# Kaggle에서 돌리기 (주력)

1. [notebooks/cati_kaggle.ipynb](notebooks/cati_kaggle.ipynb) 를 Kaggle에 업로드
   (New Notebook → File → Import Notebook)
2. **Settings → Accelerator → GPU T4 x2** · Internet **On**
3. 맨 위 셀의 `HF_USER` 에 Hugging Face 사용자명 입력
4. **Save Version → Save & Run All** → 창 닫기

**그 다음부터는 Save & Run All 만 다시 누르면 됩니다.**

세션 12시간, 주 30시간. 창을 닫아도 백그라운드에서 계속 돕니다.

---

# Colab에서 이어받기 (선택)

Colab 한도가 회복됐을 때만. Kaggle 세션이 끝난 뒤에 하세요.

1. Colab → 파일 → 노트북 열기 → **GitHub** 탭 → `foeplob11-code/Cati`
   → `notebooks/cati_colab.ipynb`
2. **런타임 → 런타임 유형 변경 → TPU v5e-1**
3. 맨 위 `HF_USER` 입력, 좌측 **🔑** 에 `HF_TOKEN` 추가
4. **런타임 → 모두 실행**

탭을 닫으면 멈춥니다. 노트북 절전도 꺼두세요.

---

# 단계 넘어가기

맨 위 숫자 하나만 바꿉니다.

```python
STEP = 1        # 1=50M   2=100M   3=200M
```

| STEP | 모델 | Kaggle T4x2 | 과학습 배수 |
|---|---|---|---|
| 1 | 50M | 약 5h | 42× |
| 2 | 100M | 약 22h | 41× |
| 3 | **200M** | **약 170h** | **75×** |

합계 약 197시간. 주 30시간이면 **6.6주**입니다. Colab을 얹으면 더 줄어듭니다.

50M·100M 을 먼저 끝내세요. 170시간을 태우기 전에 파이프라인을 검증하고
스케일링 법칙으로 200M 설정이 맞는지 확인하는 단계입니다.

---

# 로그에서 볼 것

```
step   100/7629  loss  7.11  |g| 0.56  lr 7.9e-04  3.0s  86,319 tok/s  MFU 12.6%  scale 3e+04 버림 0
```

| | 정상 | 이상 |
|---|---|---|
| `loss` | 10.8에서 시작해 감소 | 안 내려가거나 `nan` |
| `MFU` | 15~25% | 10% 미만 |
| `scale` | 안정적, 가끔 2배 | 자주 반토막 → 학습률 높음 |
| `버림` | 전체의 1% 미만 | 20% 넘으면 자동 중단 |

**첫 세션 끝나면 MFU 숫자를 알려주세요.** 그걸로 200M 설정을 확정합니다.

---

# 설정 실험하기

73시간을 태우기 전에 짧게 재볼 수 있습니다.

```bash
python scripts/train.py --tier configs/tier2_200m.json --bench 20
python scripts/train.py --tier configs/tier2_200m.json --bench 20 --set micro_batch_per_device=8
```

`--bench` 는 체크포인트를 저장하지 않아 본 학습에 영향이 없습니다.
