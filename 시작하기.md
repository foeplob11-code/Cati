# 학습 시작하기

## 준비 — 한 번만

1. https://www.kaggle.com/settings → API → **Create New Token** → `kaggle.json` 다운로드
2. 터미널에 두 줄 붙여넣기

```bash
mkdir -p ~/.kaggle && mv ~/Downloads/kaggle.json ~/.kaggle/ && chmod 600 ~/.kaggle/kaggle.json
```

## 학습

```bash
./train
```

끝입니다. 브라우저를 열 필요 없습니다.

---

## 그 다음부터

**`./train` 만 다시 치면 됩니다.** 체크포인트에서 알아서 이어집니다.

| 명령 | 하는 일 |
|---|---|
| `./train` | 학습 시작 / 이어하기 |
| `./train status` | 지금 돌고 있는지 확인 |
| `./train log` | 로그 보기 |
| `./train watch` | 끝날 때까지 지켜보기 |

## 단계 넘어가기

로그 마지막에 이렇게 나오면

```
┌─────────────────────────────────────────────┐
│  cati-50m 완료                               │
│  → 맨 위 STEP 을 2 로 바꾸고 다시 실행        │
└─────────────────────────────────────────────┘
```

이렇게 치면 됩니다.

```bash
./train 2
```

| 명령 | 모델 | 걸리는 시간 |
|---|---|---|
| `./train 1` | 50M | 1시간 |
| `./train 2` | 100M | 4.5시간 |
| `./train 3` | 350M | 5주 (`./train` 을 11번) |

---

## 350M 시작할 때만

체크포인트가 세션 밖에서 살아남아야 5주를 이어갈 수 있습니다.
Kaggle 노트북 페이지 → **Add-ons → Secrets** 에 2개 추가:

- `KAGGLE_USERNAME` — `kaggle.json` 안의 username
- `KAGGLE_KEY` — `kaggle.json` 안의 key

50M·100M은 한 세션에 끝나므로 없어도 됩니다.

---

## 알려주실 숫자 하나

50M이 끝나면 `./train log` 에서 **MFU** 만 알려주세요.

- 35% 근처 → 그대로 진행
- 25% 미만 → 350M 토큰 수를 낮춰야 합니다
