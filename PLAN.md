# Cati — 오프라인 AI 비서 계획서 (v0.4)

**환경 확정** · 배포: MacBook M1 / 16GB / macOS 26.5 · 학습: Kaggle 무료 티어 (TPU v3-8)
**경로 확정** · **경로 A — 350M dense** (2026-07-25 결정)

---

## 0. 확정 사양

| 항목 | 값 | 근거 |
|---|---|---|
| 최종 모델 | **cati-350m** (343.5M dense) | Kaggle 쿼터 내 완주 가능한 최대 규모 |
| 학습 토큰 | 25B (**73배 과학습**) | 소형 모델은 Chinchilla 최적점을 크게 넘겨야 쓸 만해진다 |
| 사다리 | **50M → 100M → 350M** | 원안 7단계 중 쿼터 내 실행 가능한 3단계 |
| 배포 형식 | **f16 GGUF, 0.64GB** | 양자화 손실 0 — 이 규모에선 1비트도 아깝다 |
| 처음부터 학습 | ✅ | 원안 유지 |
| TPU 소요 | **102.9h / 160h** (버퍼 36%) | `scripts/budget.py`로 검증 |

> 원안의 "2.5B dense"는 Kaggle 예산의 **12배**를 요구해 불가능하다.
> 2.5GB라는 *용량*은 유지하되, 그 공간을 모델 파라미터가 아니라
> **오프라인 지식 인덱스**에 쓴다 (§3). 이 규모에서 비서를 쓸모있게 만드는 건
> 모델 크기가 아니라 검색 + 도구 호출이다.

### 검증된 티어 (`python3 scripts/budget.py`)

| 티어 | 전체 | 비임베딩 | 임베딩% | 토큰 | 배수 | TPU-h | 역할 |
|---|---|---|---|---|---|---|---|
| cati-50m | 47.7M | 22.6M | 52.7% | 2.0B | 42× | 1.1 | 파이프라인 검증. 버린다 |
| cati-100m | 97.8M | 66.4M | 32.2% | 4.0B | 41× | 4.4 | **제품의 의도 분류 라우터로 재활용** |
| cati-350m | 343.5M | 293.1M | 14.7% | 25.0B | 73× | 97.4 | **최종 출시 모델** |
| | | | | | **합계** | **102.9** | 버퍼 57h |

> ⚠️ 50M 티어는 파라미터의 **52.7%가 임베딩**이라 스케일링 법칙 점으로는 편향돼 있다.
> 법칙 피팅은 반드시 **비임베딩 파라미터** 기준으로 할 것.

---

## 1. Kaggle 계산 예산

| 자원 | 스펙 | 쿼터 | 세션 한계 | 실효 |
|---|---|---|---|---|
| **TPU v3-8** | 8코어 / HBM 128GB | ~20 h/주 | 9시간 | ~147 TFLOPS (peak 420 × MFU 35%) |
| GPU 2×T4 | 16GB×2 | 30 h/주 | 12시간 | ~24 TFLOPS |
| GPU P100 | 16GB, 텐서코어 없음 | 30 h/주 | 12시간 | ~7 TFLOPS |

- TPU 1시간 ≈ 5.3e17 FLOPs → 8주 총 **8.5e19 FLOPs**
- GPU는 TPU의 1/6 수준. 데이터 전처리·평가·소규모 ablation 용도로만 쓴다.

> ⚠️ **쿼터는 이월되지 않는다.** 주 20시간이 상한이므로 97시간짜리 350M 런은
> GPU가 남아돌아도 **최소 5주의 벽시계 시간**이 필요하다. 이것이 일정의 실제 제약이다.

**W1 최우선 과제: 쿼터 실측.** 위 수치는 추정이다. 실측 MFU가 25% 이하로 나오면
350M 토큰 수를 25B → 18B로 즉시 하향한다.

---

## 2. 토크나이저 — 가장 투자 대비 효과가 큰 결정

### 실측 기준선 (2026-07-25, 한국어 프로브 104자)

| 토크나이저 | vocab | 글자/토큰 |
|---|---|---|
| Qwen3-1.7B | 151,669 | 1.39 |
| **SmolLM2-360M** | **49,152** | **0.47** |
| GPT-2 | 50,257 | 0.46 |
| **cati (목표)** | **49,152** | **≥ 2.0, 목표 2.5** |

**SmolLM2는 vocab이 우리와 정확히 같은데 한국어에서 0.47이다** — 사실상 바이트 폴백.
같은 vocab 예산으로 한국어 중심 학습을 하면 4~5배를 얻을 수 있다.

### 왜 결정적인가

계산량은 **토큰** 단위로 붙는다 (FLOPs = 6ND). 압축률이 좋아지면
같은 25B 토큰 예산에 **더 많은 원문**이 들어간다.

| 압축률 | 25B 토큰의 한국어 원문 | 2048토큰 컨텍스트 |
|---|---|---|
| 0.47 (SmolLM2급) | 11.8B 자 | 963 자 |
| 1.39 (Qwen3급) | 34.8B 자 | 2,847 자 |
| **2.5 (목표)** | **62.5B 자** | **5,120 자** |

같은 GPU 시간으로 **5.3배 많은 한국어를 학습**하고, 컨텍스트도 5배 길어진다.

### 설정 (`configs/tokenizer.json` — 확정)

| 항목 | 값 | 이유 |
|---|---|---|
| vocab | 49,152 | 128의 배수(TPU 효율) & 65,536 미만(uint16 저장) |
| 정규화 | **NFC** | 한글 조합/분해형 통일. macOS는 NFD를 쓴다 |
| 모델 | 바이트 레벨 BPE | 256바이트 전부 초기 알파벳 → UNK 원천 차단 |
| 숫자 | 한 자씩 분리 | 산술 일관성 |
| 학습 믹스 | 한 50% / 영 35% / 코드 15% | 한국어 병합을 의도적으로 우대 |
| 특수 토큰 | 도구 호출용 미리 예약 | 나중에 추가하면 임베딩 재학습이 필요하다 |

> ⚠️ **W1에 확정하고 전 티어가 공유한다.** 중간에 바꾸면 사다리 실험이 전부 비교 불가능해진다.

---

## 3. 2.5GB 용량 배분

| 구성요소 | 용량 | 비고 |
|---|---|---|
| cati-350m, **f16** | 0.64 GB | 양자화 손실 0 |
| cati-100m 라우터, Q8 | 0.10 GB | 의도 분류 |
| 임베딩 모델 (30M급) | 0.05 GB | 오프라인 검색 |
| **오프라인 지식 인덱스** | **1.40 GB** | ← 여기가 실사용 품질을 결정한다 |
| llama.cpp 런타임 (정적) | 0.03 GB | |
| Tauri 셸 + 마스코트 + 폰트 | 0.05 GB | Electron이면 +150MB → 채택 불가 |
| **합계** | **2.27 GB** | |

**KV 캐시(별도 RAM)**: 토큰당 26.0KB → 8K 컨텍스트에서 **208MB**. M1 16GB에서 여유.
**M1 추론 속도 추정**: 토큰당 0.64GB 읽기 / 대역폭 68GB/s → 이론 106 tok/s, 실측 60~90 예상.

> 25B 토큰 모델은 **지식이 얇다.** 한국어 문장은 유창하게 쓰지만 사실 관계는 신뢰할 수 없다.
> 이건 리스크가 아니라 확정된 특성이다. 1.4GB 지식 인덱스 + 도구 호출이 이걸 메우는 설계다.

---

## 4. 학습

### 4.1 데이터 — 스토리지 문제와 해법

25B 토큰을 uint16으로 저장하면 50GB. 로컬 디스크 여유는 55GB뿐이라 둘 수 없다.

**해법: 하이브리드**
- **웹 대량 코퍼스는 HF `datasets` 스트리밍 + 실시간 토크나이징.**
  350M 모델의 소비 속도는 약 **71,000 tok/s ≈ 원문 0.3 MB/s**. 스트리밍으로 충분하다.
- **도서/어닐링 코퍼스만 사전 토크나이징** 후 Kaggle Dataset으로 업로드.
  작고(수 GB) 반복 사용되므로 이쪽이 유리하다.

| 코퍼스 | 언어 | 방식 | 라이선스 |
|---|---|---|---|
| FineWeb2 (kor_Hang) | 한 | 스트리밍 | ODC-By |
| FineWeb-Edu | 영 | 스트리밍 | ODC-By |
| 한국어 위키 | 한 | 사전 토큰화 | CC BY-SA |
| 모두의 말뭉치 / AI Hub | 한 | 사전 토큰화 | 신청 필요 |
| 공유마당 (저작권 만료 도서) | 한 | 사전 토큰화 | 퍼블릭 도메인 |
| Project Gutenberg | 영 | 사전 토큰화 | 퍼블릭 도메인 |
| The Stack v2 (필터) | 코드 | 스트리밍 | 각 라이선스 |

> ⚠️ **저작권**: 배포 제품이므로 만료 저작물·명시 라이선스만 사용.
> 나무위키 덤프는 CC BY-NC-SA(비영리)라 **상용 배포 불가**.

한국어 목표 비중 40% → 10B 토큰. 압축률 2.5면 원문 25B자로 공개 코퍼스에서 충분히 확보되고,
부족하면 **2~3 에폭 반복**(연구상 4에폭까지는 손실 거의 없음).

### 4.2 "책으로 학습해서 글을 잘 쓰게" — 적용 위치

책만으로 사전학습하면 지식·지시이행·도구사용이 전부 무너진다.
책은 **마지막 15% 어닐링 구간**에 배치하는 것이 정석이며, 문체 향상 효과도 이쪽이 더 크다.

```
[1] 사전학습 85% (21.25B tok) : 웹(ko/en) + 위키 + 코드
[2] 어닐링   15% ( 3.75B tok) : 도서 · 문학 · 에세이   ← 문체가 여기서 결정된다
                                학습률을 0으로 감쇠 + 컨텍스트 2K→8K 확장
[3] SFT      (~100M tok)      : 지시이행 + MCP 도구호출 궤적 + 글쓰기 지시
[4] DPO      (~10M tok)       : 문체 A/B 선호 정렬
```

### 4.3 아키텍처 (`configs/tier*.json` — 검증됨)

| 항목 | 선택 |
|---|---|
| 구조 | Llama 계열 디코더 · RMSNorm · SwiGLU · RoPE |
| 어텐션 | GQA — 350M은 16 헤드 / KV 4 헤드 / head_dim 64 |
| 350M 형상 | d_model 1024 · 26층 · d_ff 2816 · 임베딩 tied |
| 옵티마이저 | AdamW (β=0.9/0.95, wd 0.1, clip 1.0) — 350M은 체크포인트 4.5GB로 20GB 한도 안 |
| 정밀도 | bf16 (TPU 네이티브) |
| 컨텍스트 | 사전학습 2048 → 어닐링에서 8192 (rope_theta 10k→500k) |
| 프레임워크 | JAX/Flax + Optax |

---

## 5. 에이전틱 — 350M의 현실적 해법

350M은 자유형식 ReAct 루프를 못 돈다. 세 장치로 **구조적으로** 보완한다.

1. **GBNF 문법 강제 디코딩** — llama.cpp grammar로 도구 호출 JSON 스키마를 강제.
   파싱 실패율을 0에 수렴시킨다. **소형 에이전트의 핵심 기술.**
2. **합성 도구 호출 데이터** — 큰 교사 모델로 MCP 호출 궤적을 대량 생성해 SFT.
   (사전학습은 처음부터, SFT 데이터는 합성 — 원안과 모순 없음)
3. **100M 라우터** — 사다리의 100M 티어를 `잡담 / 도구 필요 / 문서 검색` 분류기로 투입.
   → 사다리의 작은 티어가 버려지지 않고 실제 부품이 된다.

---

## 6. 제품 아키텍처

```
┌──────────────────────────────────────────────┐
│  Tauri 앱 셸 (Rust + 웹 UI)                   │
│  ┌────────────────────────────────────────┐  │
│  │ UI: 채팅 · 마스코트 · 설정(1페이지)      │  │
│  ├────────────────────────────────────────┤  │
│  │ 에이전트 루프 (Rust)                    │  │
│  │  계획 → 도구호출 → 관찰 → 반복           │  │
│  │  GBNF 강제 · 실행 승인 게이트            │  │
│  ├──────────────┬──────────────┬──────────┤  │
│  │ llama.cpp    │ MCP 클라이언트 │ 100M     │  │
│  │ (GGUF/Metal) │ (stdio+HTTP)  │ 라우터   │  │
│  ├──────────────┴──────────────┴──────────┤  │
│  │ 오프라인 지식 인덱스 (SQLite + HNSW)     │  │
│  └────────────────────────────────────────┘  │
└──────────────────────────────────────────────┘
```

**기본 내장 도구** (MCP 없이도 즉시 동작 — "편의성" 요구사항):
파일 읽기/쓰기 · 문서 검색 · 일정/메모 · 클립보드 · 앱 실행 · 계산

---

## 7. 디자인

모던 · 심플 · 여백 중심. 채팅창 하나로 모든 기능 접근. 설정은 1페이지.

### 마스코트 — 하얀 고양이 "Cati"
2D 벡터, 최소한의 선. 흰 몸 + 포인트 컬러 1개.
**상태를 텍스트가 아니라 마스코트가 말한다:**

| 상태 | 표현 |
|---|---|
| idle | 눈 감고 천천히 호흡 |
| listening | 귀 쫑긋 |
| thinking | 꼬리 좌우 흔들기 |
| working (도구 실행) | 앞발 움직임 |
| done | 눈 반달 |
| error | 귀 접힘 |

시스템 트레이 아이콘도 동일 상태 반영.

---

## 8. 8주 로드맵 (TPU 주 20h 상한 반영)

| 주 | 학습 트랙 (TPU) | 제품 트랙 (M1 로컬) |
|---|---|---|
| **W1** | **쿼터 실측** · **체크포인트/재개 인프라** · 토크나이저 확정 · 50M(1.1h) · 100M(4.4h) · 350M 착수 (14h) | 툴체인 설치 · Tauri 셸 + llama.cpp 연동 |
| **W2** | 350M 본 학습 (20h) · 스케일링 법칙 피팅(비임베딩 기준) | 채팅 UI · 마스코트 상태 머신 · 디자인 시스템 |
| **W3** | 350M (20h) | MCP 클라이언트 · GBNF 도구호출 강제 |
| **W4** | 350M (20h) | 에이전트 루프 · 승인 게이트 · 내장 도구 |
| **W5** | 350M (20h) | 오프라인 지식 인덱스 구축 · 100M 라우터 통합 |
| **W6** | 350M 사전학습 완료 (3.4h) → **도서 어닐링** 착수 | 온보딩 · 설정 · 통합 테스트 |
| **W7** | 어닐링 완료 · 컨텍스트 8K 확장 · SFT · DPO | Metal 튜닝 · 성능 최적화 |
| **W8** | 평가 · GGUF f16 변환 · 최종 검증 | 패키징 · 설치 관리자 · 배포 |

> 두 트랙은 **GGUF 파일 하나로만 결합**한다. 학습이 밀려도 제품 개발은 멈추지 않는다.
> 버퍼 57h는 W1 실측 결과에 따라 350M 토큰 수를 늘리거나(품질↑) 재시작을 흡수하는 데 쓴다.

---

## 9. 리스크

| 리스크 | 심각도 | 대응 |
|---|---|---|
| 9h 세션에서 체크포인트 유실 | **높음** | W1에 재개 인프라를 학습 코드보다 **먼저** 구축 · 8.5h 강제 저장 |
| 실측 MFU가 35% 미만 | **높음** | W1 실측 → 토큰 25B→18B 즉시 하향 (버퍼 57h로 흡수) |
| FineWeb2 config 이름 변경 | 중간 | W1에 유효성 확인 · 소스를 설정으로 분리해둠 |
| 한국어 압축률 목표 미달 | 중간 | 한국어 믹스 비중 50%→65% 상향 후 재학습 (토크나이저 학습은 저렴) |
| 도서 저작권 | 중간 | 만료 저작물·명시 라이선스만 |
| 모델 지식이 얇음 | **확정 발생** | 1.4GB 오프라인 지식 인덱스 + GBNF 도구 호출로 구조적 보완 |
| 학습 실패 | 중간 | 100M 체크포인트로도 출시 가능한 상태 유지 |

---

## 10. 로컬 환경

| 항목 | 상태 |
|---|---|
| M1 / 16GB / 디스크 여유 55GB | ✅ 추론 충분 |
| Python 3.9.6 (Xcode 번들) | ⚠️ 3.11+ 필요 |
| Rust | ❌ Tauri용 설치 필요 |
| Node v22.23.1 / cmake 4.4.0 / git 2.50.1 / Homebrew | ✅ |
| `.venv` + tokenizers 0.22.2 | ✅ 설치됨 |

> 로컬에는 토크나이즈된 대량 코퍼스를 두지 않는다 (§4.1 스트리밍 전략).
